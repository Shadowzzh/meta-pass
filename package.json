{
  "name": "meta-pass",
  "author": "ziheng",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "dev:github": "pnpm --filter frontend dev:github",
    "build": "pnpm --filter frontend build",
    "build:github": "pnpm --filter frontend build:github",
    "hardhat-clean": "pnpm --filter contract clean",
    "hardhat-compile": "pnpm --filter contract compile",
    "hardhat-node": "pnpm --filter contract node",
    "hardhat-deploy": "pnpm --filter contract deploy-local",
    "sepolia-deploy": "pnpm --filter contract deploy-sepolia",
    "prettier": "prettier --write '**'"
  },
  "engines": {
    "node": "20.x",
    "pnpm": "9.x"
  }
}
