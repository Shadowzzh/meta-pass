{
  "name": "meta-pass",
  "author": "ziheng",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "hardhat-clean": "(cd packages/contract && npx hardhat clean)",
    "hardhat-compile": "(cd packages/contract && npx hardhat compile)",
    "hardhat-node": "(cd packages/contract && npx hardhat node)",
    "hardhat-deploy": "(cd packages/contract && npx hardhat ignition deploy ./ignition/modules/TicketSystem.ts --network localhost)",
    "prettier": "prettier --write '**'"
  },
  "engines": {
    "node": "20.x",
    "pnpm": "9.x"
  }
}
